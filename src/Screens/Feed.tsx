import React from 'react';
import {View, SafeAreaView, Text, ScrollView} from 'react-native';

export const Post = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text> Posts </Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};
