import React from 'react';
import {View, SafeAreaView, Text, ScrollView} from 'react-native';

export const Calendar = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>Calendário e agenda de eventos municipal</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};
